import { http } from './http'
import { setToken, getToken, removeToken } from './token'
import { setUsername, getUsername, removeUsername } from './user'

export {  http, 
    setToken, getToken, removeToken, 
    setUsername, getUsername, removeUsername
 }